<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Sample_Activity" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Sample_Activity" parent="Base.Theme.Sample_Activity" />
    <style name="primaryCalculator" parent="ThemeOverlay.Material3.Button">
        <item name="backgroundTint">@color/primaryColor</item>
        <item name="cornerRadius">12dp</item>
        <item name="android:textColor">@color/white</item>
    </style>
    <style name="digitCalculatorButton" parent="ThemeOverlay.Material3.Button">
        <item name="backgroundTint">@color/darkGray</item>
        <item name="cornerRadius">12dp</item>
        <item name="android:textColor">@color/white</item>
    </style>
    <style name="SecondaryCalculator" parent="ThemeOverlay.Material3.Button">
        <item name="backgroundTint">@color/Gray</item>
        <item name="cornerRadius">12dp</item>
        <item name="android:textColor">@color/primaryColor</item>
    </style>
    <style name="Splash_Activity" parent="ThemeOverlay.Material3.Button">
        <item name="backgroundTint">@color/Gray</item>
        <item name="cornerRadius">12dp</item>
        <item name="android:textColor">@color/primaryColor</item>
    </style>
</resources>